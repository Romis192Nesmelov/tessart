<div class="phone_block">
    <div class="prefix">{{ Settings::getMainSettings()->main_phone_prefix }}</div>
    <div class="phone">{{ Settings::getMainSettings()->main_phone }}</div>
    <div><a href="#feedback_modal" data-toggle="modal">{{ trans('content.send_request') }}</a></div>
</div>